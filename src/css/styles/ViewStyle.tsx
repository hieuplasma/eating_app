import { StyleSheet } from "react-native";

export const viewStyle = StyleSheet.create({
    container: {
        flex: 1,
        padding: 12
    },
    center: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    row: {
        flexDirection: 'row',
        alignItems: 'center'
    }
})